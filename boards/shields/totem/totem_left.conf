# Left half is the BLE central — it manages host connections.
#
# LED assignment is in totem_left.overlay:
#   led1 (green) = zmk,battery-level-status-led  (event-triggered, battery-safe)
#
# CONFIG_ZMK_BATTERY_LEVEL_STATUS is intentionally omitted here.
# It may not exist under that exact name in ZMK main and would break the build.
# To enable LED battery level display, verify the correct Kconfig symbol name
# in ZMK's app/Kconfig and add it here once confirmed.
